{% extends '::default/Admin.html.twig' %}

{% block body %}
    <h1>Créer une catégorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Créer" />
    {{ form_end(form) }}

            <a class="btn btn-primary" href="{{ path('adminCategorie_index') }}">Retour à la liste</a>

{% endblock %}
