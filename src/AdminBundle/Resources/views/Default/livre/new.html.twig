{% extends '::default/Admin.html.twig' %}

{% block body %}
    <h1>Livre creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="Créer" />
    {{ form_end(form) }}

            <a class="btn btn-primary" href="{{ path('adminLivre_index') }}">Retour à la liste</a>

{% endblock %}
